
public class MainProgram {

    // update here your exercise progress
    public static int partsCompleted() {
        ExerciseManagement management = new ExerciseManagement();
        int partsCompleted = management.exerciseList().size();
        if (partsCompleted == 0){
            return 0;
        }
        if (partsComplete)
        return 0;
    }
}
